# Global Environment Variables for Martin's Homelab
# Copy this file to .env and fill in your values

# Core Configuration
# Your base domain name - used across all stacks
DOMAIN_NAME=example.com

# User/Group IDs for containers (typically 1000 for primary user)
PUID=1000
PGID=1000

# Timezone
TZ=Europe/Oslo

# Database Credentials (for databases stack and dependent services)
DB_USERNAME=dbuser
DB_PASSWORD=change-this-secure-password

# Traefik Configuration
# Generate with: htpasswd -nb username password
# Remember to escape $ as $$ in compose files
TRAEFIK_DASHBOARD_CREDENTIALS=user:$$apr1$$hashedpassword

# Homepage Dashboard Configuration
HOMEPAGE_ALLOWED_HOSTS=

# Add service-specific variables here or in individual stack .env files
# See each stack's README.md and stack.env for available options
