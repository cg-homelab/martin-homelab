# Note: This file is OUTDATED and not used by the current compose file.
# See README.md for required configuration.

# Cloudflare API token should be in: /mnt/fast/appdata/traefik/cf_api_token.txt

# Traefik dashboard credentials (generate with: htpasswd -nb username password)
# Remember to escape $ as $$ in the compose file
TRAEFIK_DASHBOARD_CREDENTIALS=user:$$apr1$$hashedpassword

# DOMAIN_NAME should be set in your environment or global .env file
DOMAIN_NAME=example.com
